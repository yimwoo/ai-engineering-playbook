# --- AGENT_PROTOCOL.md runtime state ---
# Everything under .agent/ is transient session scratch except the bootstrap
# sentinel, which must be tracked so the re-bootstrap guard in Phase 1 works.
.agent/*
!.agent/bootstrapped

# --- Local agent tool configs (keep out of origin) ---
.agenteam/
.codex/

# macOS
.DS_Store
